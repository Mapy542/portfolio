{
	"$id": "toolsMeta.schema.json",
	"$schema": "http://json-schema.org/draft-07/schema#",
	"title": "Tools Page Metadata",
	"description": "Metadata for static pages keyed by page identifier.",
	"type": "object",
	"patternProperties": {
		"^.+$": {
			"type": "object",
			"description": "Metadata for a single static page.",
			"required": ["url", "title", "description", "image", "date"],
			"properties": {
				"url": {
					"type": "string",
					"pattern": "^/",
					"description": "Absolute path/URL for the page (must start with /)."
				},
				"title": {
					"type": "string",
					"minLength": 1,
					"description": "Display title for the page."
				},
				"description": {
					"type": "string",
					"description": "Brief description of the page content."
				},
				"image": {
					"type": "string",
					"description": "Relative path to the page's representative image (can be empty)."
				},
				"date": {
					"type": "string",
					"description": "Date string for the page (can be empty)."
				}
			},
			"additionalProperties": false
		}
	},
	"additionalProperties": false
}
